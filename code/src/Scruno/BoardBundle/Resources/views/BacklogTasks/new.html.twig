{% extends '::base.html.twig' %}

{% block body -%}
    <h1>BacklogTasks creation</h1>

    <form action="{{ path('backlog_foo__create') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

        <ul class="record_actions">
    <li>
        <a href="{{ path('backlog_foo_') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
